This applications creates a space in Webex were it listens for a question on the subnet of networking. 
Bot lisenting sends query to application which send question to ChatGPT for insights.
Applications returns ChatGPT insights into WebEx space.
